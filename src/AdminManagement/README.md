# Admin Management - البنية الجديدة

## 📁 هيكل المجلدات

```
AdminManagement/
├── components/          # المكونات المشتركة
│   ├── LawCard.jsx
│   ├── LawFormDialog.jsx
│   └── StyledComponents.js
├── services/           # خدمات API
│   ├── api.js
│   ├── lawsService.js
│   └── usersService.js
├── hooks/              # Custom Hooks
│   └── useLaws.js
├── utils/              # الأدوات والثوابت
│   └── constants.js
└── styles/             # ملفات الأنماط (للمستقبل)
```

## 🎯 المبادئ

### 1. فصل الاهتمامات (Separation of Concerns)
- **Components**: المكونات UI فقط
- **Services**: منطق API والاتصال بالخادم
- **Hooks**: منطق إدارة الحالة والبيانات
- **Utils**: الثوابت والأدوات المساعدة

### 2. إعادة الاستخدام (Reusability)
- المكونات قابلة لإعادة الاستخدام
- الخدمات موحدة لجميع الصفحات
- الـ Hooks قابلة للاستخدام في أي مكان

### 3. سهولة الصيانة (Maintainability)
- كل ملف له مسؤولية واحدة
- الكود منظم وسهل القراءة
- سهولة إضافة ميزات جديدة

## 📝 كيفية الاستخدام

### استخدام الخدمات (Services)
```javascript
import { lawsService } from './services/lawsService';

// جلب القوانين
const laws = await lawsService.getAll();

// إنشاء قانون جديد
await lawsService.create(lawData);
```

### استخدام الـ Hooks
```javascript
import { useLaws } from './hooks/useLaws';

function MyComponent() {
  const { laws, loading, error, refetch } = useLaws(currentTab, searchQuery);
  // ...
}
```

### استخدام المكونات
```javascript
import LawCard from './components/LawCard';
import { colors } from './utils/constants';
```

## 🔄 خطوات الترحيل

1. ✅ إنشاء البنية الأساسية
2. ✅ إنشاء ملفات الخدمات والثوابت
3. ✅ إنشاء المكونات المشتركة
4. ⏳ تحديث LawsManagement.jsx
5. ⏳ تحديث باقي الصفحات تدريجياً

## 📌 ملاحظات

- الملف `LawsManagement.refactored.jsx` هو مثال على البنية الجديدة
- يمكن استخدامه كمرجع عند تحديث باقي الصفحات
- جميع الملفات القديمة لا تزال تعمل حتى يتم الترحيل الكامل

